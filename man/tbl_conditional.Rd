% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_conditional.R
\name{tbl_conditional}
\alias{tbl_conditional}
\title{Conditional Weighted Frequency Table}
\usage{
tbl_conditional(
  x_vector,
  conditions = list(named = TRUE),
  w_vector = NULL,
  useNA = "no",
  total = TRUE,
  digits = 1
)
}
\arguments{
\item{x_vector}{A vector of the main variable to be analyzed.}

\item{conditions}{A named list of logical vectors indicating the conditions for each subset.}

\item{w_vector}{A vector of weights. If NULL, equal weights are assumed.}

\item{useNA}{How to handle NA values. Options are 'no', 'ifany', or 'always'. Default is 'always'.}

\item{total}{Logical. Should the total be included in the output? Default is TRUE.}

\item{digits}{Number of decimal places to include in the results. Default is 1.}
}
\value{
A data frame with the frequency tables for each condition and the total.
}
\description{
This function computes conditional weighted frequency tables for specified conditions.
}
\examples{
# Example usage
x <- sample(c("Yes", "No"), 100, replace = TRUE)
y <- sample(c("Male", "Female"), 100, replace = TRUE)
z <- sample(c("Worker", "Inactive"), 100, replace = TRUE)
w <- runif(100, 0.5, 1.5)
df <- data.frame(x, y, z, w)
cond <- list(
  "Male" = df$y \%in\% "Male",
  "Male Worker" = df$y \%in\% "Male" & df$z \%in\% "Worker",
  "Female" = df$y \%in\% "Female",
"Female Inactive" = df$y \%in\% "Female" & df$z \%in\% "Inactive"
)
tbl_conditional(df$x, cond, df$w)

}
